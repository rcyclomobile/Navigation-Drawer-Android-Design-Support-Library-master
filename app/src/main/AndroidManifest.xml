<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.videumcorp.desarrolladorandroid.navigationdrawerandroiddesignsupportlibrary" >

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".FullscreenActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="Mi Perfil"
            android:parentActivityName=".SelectEstablishmentActivity$CompanyLoginActivity" >
        </activity>
        <activity
            android:name=".SelectEstablishmentActivity$CompanyLoginActivity"
            android:label="Escoger actividad" >
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".CompanyMainActivity"
            android:label="Menu" >
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".CompanyRegisterActivity"
            android:label="R-Cyclo"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".SelectWasteActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".SelectEstablishmentActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme" >
        </activity>

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".SelectCoordinatesActivity"
            android:label="@string/title_activity_select_coordinates" >
        </activity>
        <activity android:name=".FormConteinerRequestActivity" >
        </activity>
        <activity
            android:name=".CompanyModifyContainerActivity"
            android:label="@string/title_activity_company_modify_container"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".EstablishmentLoginActivity"
            android:label="@string/title_activity_establishment_login"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".EstablishmentRegisterActivity"
            android:label="@string/title_activity_establishment_register"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".EstablishmentMainActivity"
            android:label="Menu"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".EditProfileActivity"
            android:label="Editar perfil"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".SolicitudPendiente"
            android:label="@string/title_activity_solicitud_pendiente"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".ChangePasswordActivity"
            android:label="Cambiar contraseña"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".AvailableContainerActivity"
            android:label="Mis Contenedores"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".SettingsEstablishmentActivity"
            android:label="Mi perfil"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".EditProfileEstablishmentActivity"
            android:label="Editar perfil"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".ChangePasswordEstablishmentActivity"
            android:label="Cambiar contraseña"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".DeleteContainerEstablishmentActivity"
            android:label="Mis contenedores"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".NewDirectionActivity"
            android:label="@string/title_activity_new_direction" >
        </activity>
        <activity
            android:name=".EditPhoneActivity"
            android:label="@string/title_activity_edit_phone"
            android:theme="@style/AppTheme" >
        </activity>
    </application>

</manifest>
